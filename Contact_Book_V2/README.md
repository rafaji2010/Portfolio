A contact management system with **four specialized indexes**:

- **BST** for alphabetical listing
- **Trie** for autocomplete
- **Hash Map** for O(1) email lookup
- **Heap** for recent contacts

## Installation

```bash
uv pip install -e ".[dev]"
